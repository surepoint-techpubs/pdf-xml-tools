# Core web framework
Flask==3.1.3
Werkzeug==3.1.8

# Security / auth
Flask-WTF==1.3.0       # CSRF protection
Flask-Limiter==4.1.1   # login rate limiting (per-IP)
bcrypt==5.0.0           # password hashing

# PDF parsing/rendering -- the only PDF-handling library in this app,
# deliberately: no Ghostscript, no ImageMagick, no poppler. Self-contained,
# no external binaries.
PyMuPDF==1.26.5

# XML fragment construction -- safe escaping/serialization, not string formatting
lxml==5.4.0

# Database is sqlite3 (Python stdlib) -- no package needed.

# Production WSGI server
gunicorn==26.0.0
